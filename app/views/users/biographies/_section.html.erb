<%# locals: (form:, heading:, fields:) %>
<section class="mt-10 border-t border-line pt-8">
  <h2 class="font-serif font-medium text-2xl tracking-tight text-ink"><%= heading %></h2>

  <% fields.each do |field| %>
    <%= render "prompt", form: form, field: field %>
  <% end %>
</section>
